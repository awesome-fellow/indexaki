general:
test:
  override:
    - npm run coverage
  post:
    - ./node_modules/.bin/codeclimate-test-reporter < ./coverage/lcov.info
deployment:
  staging:
    branch: develop
    heroku:
      appname: betaindexaki
      skip_cleanup: true
  release:
    tag: /v*/
    owner: circleci
    branch: master
    heroku:
      appname: indexaki
      skip_cleanup: true