general:
  artifacts:
  - "coverage"
test:
  post:
    - bash: <(./node_modules/.bin/codeclimate-test-reporter < coverage/lcov-report/lcov.info)
deployment:
  staging:
    branch: master
    heroku:
      appname: indexaki
  release:
    tag: /release-.*/
    owner: circleci
    branch: master
    heroku:
      appname: indexaki