general:
  artifacts:
  - "coverage"
test:
  override:
    - npm run coverage
  post:
    - ./node_modules/.bin/codeclimate-test-reporter < ./coverage/lcov.info
deployment:
  staging:
    branch: develop
    heroku:
      appname: betaindexaki
  release:
    tag: /release-.*/
    owner: circleci
    branch: master
    heroku:
      appname: indexaki